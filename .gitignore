/*                 # 기본은 다 무시
!/.gitignore       # .gitignore는 추적

# templates 전체 허용(디렉터리 자체 + 내부 모든 파일)
!/templates/
!/templates/**

# 루트에 있는 .py 파일만 허용
!/*.py

# 선택적으로 허용할 파일들
!/README.md
!/requirements.txt

# 절대 올리지 말 것(비밀/키)
.env
*.env
