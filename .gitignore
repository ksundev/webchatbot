# 1) 기본적으로 전부 무시
/*

# 2) 하지만 아래는 허용(추가)
!/.gitignore

# templates 폴더 전체 허용 (하위 파일/폴더까지)
!/templates/**
# 루트에 있는 .py 파일만 허용 (하위 폴더의 .py는 제외)
!/*.py

# 선택: 문서 몇 개는 허용하고 싶다면 추가
!/README.md
!/requirements.txt

# 절대 올리면 안 되는 비밀키/환경파일
*.env
.env
